# פרומפט לסוכן דפדפן — QA לאפליקציית legal_chat.html

הדבק את הטקסט הבא לסוכן הדפדפן (browser-use agent / Claude with computer use / כל סוכן עם יכולת גלישה ואינטראקציה):

---

## משימה

בצע בדיקת QA מקיפה לאפליקציית ווב בשם "ייעוץ משפטי · תיק הלר–גפנר" בכתובת:

```
https://htmlpreview.github.io/?https://raw.githubusercontent.com/dovheller/peer-proxy/claude/awesome-hypatia-jj5n3c/legal_chat.html
```

זוהי אפליקציית צ'אט מבוססת Claude API, בעברית, RTL, המיועדת למשתמשת לא-טכנית (אסתי) לניהול תיק משפטי בבית דין רבני. האפליקציה מורכבת מקובץ HTML יחיד (Vanilla JS, ללא framework), קוראת ל-API של Anthropic ישירות מהדפדפן (`https://api.anthropic.com/v1/messages`) או דרך proxy אופציונלי, ושומרת הגדרות ב-localStorage.

**אל תזין מפתח API אמיתי.** אם נדרש מפתח לבדיקת זרימת השיחה בפועל, ציין זאת כממצא נפרד ("לא ניתן היה לבדוק קריאות API חיות ללא מפתח") ובדוק את כל מה שניתן ללא קריאת רשת אמיתית (טפסים, ולידציה, מצבי שגיאה, רינדור, נגישות, רספונסיביות).

---

## תחומי בדיקה

### 1. טעינה ראשונית
- [ ] הדף נטען ללא שגיאות קונסולה (פתח DevTools → Console, תעד כל שגיאה/אזהרה)
- [ ] הפונט Heebo נטען כראוי (אין נפילה לפונט ברירת מחדל)
- [ ] כיוון RTL תקין בכל האלמנטים (טקסט, יישור, סדר אייקונים)
- [ ] חלון ההגדרות (Setup Modal) נפתח אוטומטית בטעינה ראשונה (כשאין localStorage)
- [ ] הודעת הפתיחה (Welcome) מוצגת כראוי עם האייקון והטקסט

### 2. חלון הגדרות חיבור (Setup Modal)
- [ ] שדה "מפתח API" מסוג `password` (לא חושף טקסט בגלוי)
- [ ] שדה "Proxy URL" מסוג `url`
- [ ] לחיצה על "שמור והתחל" ללא מילוי אף שדה → מציגה alert מתאים ואינה סוגרת את החלון
- [ ] מילוי מפתח API בלבד → נשמר, החלון נסגר
- [ ] מילוי Proxy URL בלבד → נשמר, החלון נסגר
- [ ] רענון הדף (F5) לאחר שמירה → החלון **לא** נפתח שוב (localStorage נשמר)
- [ ] כפתור "⚙ חיבור" בכותרת פותח מחדש את החלון עם הערכים הקודמים ממולאים מראש
- [ ] בדוק מה קורה אם משנים ערך ולוחצים "שמור" שוב — האם מתעדכן כראוי?

### 3. ממשק הצ'אט — קלט
- [ ] תיבת הטקסט (`textarea`) מתרחבת אוטומטית עם הקלדת טקסט ארוך (`autoResize`), עד גובה מקסימלי (120px)
- [ ] Enter (ללא Shift) שולח הודעה
- [ ] Shift+Enter יוצר שורה חדשה בלי לשלוח
- [ ] שליחת הודעה ריקה (רווחים בלבד) — לא אמורה להישלח
- [ ] לחיצה על כפתור השליחה (➤) בזמן שדה ריק — בדוק התנהגות
- [ ] כפתור השליחה מושבת (`disabled`) בזמן טעינה (`isLoading`)

### 4. כפתורי שאלות מהירות (Sidebar)
- [ ] כל אחד מ-8 הכפתורים ב"שאלות מהירות" שולח את הטקסט המתאים מיידית (חוץ מהבאים)
- [ ] הכפתורים "📄 הכן בקשה רשמית" ו-"✍ תצהיר" (בתחתית, ליד תיבת הקלט) **לא** שולחים מיידית — הם רק ממלאים את התיבה (כי הטקסט מסתיים ב-": ") ומשאירים פוקוס למשתמשת להשלים
- [ ] בדוק את הלוגיקה הזו בקפידה: `if (!text.endsWith(": ")) sendMessage();` — וודא שזה אכן ההתנהגות בפועל

### 5. צירוף קובץ
- [ ] כפתור "📎 צרף קובץ" פותח דיאלוג בחירת קובץ מערכת
- [ ] בחירת קובץ `.txt` — מוצג שבב (chip) עם שם הקובץ ליד הכפתורים
- [ ] לחיצה על ✕ בשבב הקובץ — מנקה את הקובץ המצורף (`clearAttach`)
- [ ] שליחת הודעה עם קובץ מצורף — בדוק (אם אפשר לבדוק עם Network tab) שתוכן הקובץ אכן מצורף לתוכן ההודעה הנשלחת ל-API
- [ ] קובץ מצורף מתנקה אוטומטית אחרי השליחה
- [ ] נסה לצרף קובץ לא-טקסטואלי (תמונה, PDF בינארי) — `FileReader.readAsText` עלול להחזיר תוכן לא קריא; בדוק אם יש קריסה או טקסט "זבל" מוצג למשתמשת
- [ ] קובץ ענק (אם זמין לבדיקה) — בדוק שהחיתוך ל-6000 תווים (`attachedText.slice(0, 6000)`) לא שובר את ה-UI

### 6. תצוגת הודעות
- [ ] בועת משתמש (ימין, רקע שחור) מול בועת עוזר (שמאל, רקע לבן) — מיושרות נכון
- [ ] רינדור Markdown בסיסי: `**מודגש**`, כותרות `#`/`##`/`###`, רשימות `-`/`*`/`•` ורשימות ממוספרות `1.` — בדוק עם תשובת דמה (אפשר להזריק טקסט דרך הקונסולה: `addBubble("assistant", "**בדיקה** \n## כותרת\n- פריט 1\n- פריט 2")`)
- [ ] **בדיקת אבטחה קריטית:** האם יש פגיעות XSS? נסה להזין בהודעת משתמש טקסט כמו `<script>alert(1)</script>` או `<img src=x onerror=alert(1)>` ובדוק אם זה מבוצע. שים לב ש-`formatText` עושה escape ל-`&<>` *לפני* שמוסיף תגיות HTML משלו (`<strong>`, `<h3>` וכו') — וודא שאין דרך לעקוף את ה-escaping (למשל ע"י ניצול סדר הפעולות בין ה-replace הראשון לאחרים)
- [ ] כפתור "⬇ ייצוא" מופיע מתחת לכל הודעת עוזר בלבד (לא מתחת להודעות משתמש)
- [ ] לחיצה על "⬇ ייצוא" — מורידה קובץ `.txt` עם שם `legal_<id>.txt`, מכיל כותרת עם תאריך עברי ואת תוכן ההודעה
- [ ] אחרי ייצוא, הכפתור משתנה זמנית ל-"✓ נשמר" ואז חוזר אחרי 2 שניות
- [ ] בדוק את `msgStore` — וודא שאין דליפת זיכרון (ה-object גדל לנצח עם כל הודעה, ללא ניקוי) — ציין כממצא אם רלוונטי לשיחות ארוכות מאוד

### 7. אינדיקטור "חושב" (Thinking)
- [ ] בזמן ההמתנה לתשובה, מוצג אנימציית נקודות עם אווטאר ⚖
- [ ] האינדיקטור מוסר כראוי הן במקרה הצלחה והן במקרה שגיאה

### 8. טיפול בשגיאות
- [ ] נתק את הרשת (DevTools → Network → Offline) ושלח הודעה — בדוק שמוצגת הודעת שגיאה ברורה בעברית ("⚠️ שגיאה: Failed to fetch...")
- [ ] שנה את כתובת ה-Proxy לכתובת לא תקינה ובדוק את הודעת השגיאה
- [ ] אם יש מפתח API שגוי/לא תקין (401) — בדוק שהשגיאה מוצגת בצורה מובנת ולא רק "undefined" גולמי

### 9. שיחה חדשה
- [ ] כפתור "✦ שיחה חדשה" מנקה את כל ההיסטוריה (`messages = []`) ומציג הודעת ברוכים הבאים מחדש
- [ ] וודא ש-`msgStore` (הקבצים לייצוא) לא נמחק — בדוק אם זו בעיה (כפתורי ייצוא ישנים עדיין יעבדו אחרי "שיחה חדשה"?)

### 10. רספונסיביות מובייל (קריטי — אסתי תשתמש מהטלפון!)
- [ ] בדוק ב-viewport של iPhone (375×667) ו-Android טיפוסי (360×800)
- [ ] בנקודת השבירה 640px — הסיידבר (`sidebar`) אמור להיעלם לגמרי (`display:none`). **זו בעיה פוטנציאלית**: בלי סיידבר, איך ניגשים לכפתורי "שאלות מהירות" ולעובדות התיק במובייל? תעד את זה כממצא UX משמעותי
- [ ] כותרת (header) — בדוק שלא נשברת/חופפת במסך צר (לב הכותרת, badge, וכפתור ⚙ חיבור)
- [ ] אזור הקלט בתחתית — נגיש מעל מקלדת וירטואלית? בדוק עם פוקוס בתיבת הטקסט במובייל אמיתי אם אפשר
- [ ] כפתורי "📎 צרף קובץ" / "📄 הכן בקשה רשמית" / "✍ תצהיר" — האם הם נשברים לשורה חדשה כראוי (`flex-wrap:wrap`) במסך צר?
- [ ] גודל אזורי לחיצה (tap targets) — כל הכפתורים גדולים מספיק למגע אצבע (מינימום ~44px)?

### 11. נגישות (Accessibility)
- [ ] בדוק ניווט מקלדת בלבד (Tab) — האם אפשר להגיע לכל הכפתורים והשדות בסדר הגיוני?
- [ ] `textarea` ושדות הקלט — יש להם `aria-label` או placeholder ברור?
- [ ] ניגודיות צבעים (contrast) — בדוק עם כלי כמו Lighthouse/axe שהטקסט הזהוב (`--gold`) על רקע קרם עומד בתקן WCAG AA
- [ ] הרץ Lighthouse Accessibility audit ותעד את הציון

### 12. ביצועים
- [ ] הרץ Lighthouse Performance audit
- [ ] בדוק זמן טעינה ראשוני (TTFB, FCP)
- [ ] שלח 20+ הודעות ברצף (אם אפשר עם mock) — בדוק שגלילת ה-messages container חלקה ואין lag

### 13. אבטחה ופרטיות (חשוב מאוד — מדובר בתיק משפטי רגיש)
- [ ] **קריטי:** מפתח ה-API מאוחסן ב-localStorage כטקסט גלוי (לא מוצפן). כל סקריפט אחר שירוץ באותו דומיין/קונטקסט (כולל הרחבות דפדפן זדוניות) יכול לגנוב אותו. תעד זאת כממצא אבטחה.
- [ ] בדוק האם יש דליפת מידע ל-console.log בקוד (`console.error("Proxy error:", err)` ב-proxy — לא רלוונטי לקוד הלקוח, אבל בדוק את ה-HTML עצמו)
- [ ] כש-`htmlpreview.github.io` משמש כ-host לקובץ — בדוק את ה-CSP וה-headers שהשירות הזה מחזיר. זהו שירות צד-שלישי לא רשמי — תעד את הסיכון שהקובץ "מוגש" דרך דומיין לא נשלט (אם htmlpreview ייפול/ישתנה, האפליקציה תפסיק לעבוד; אם הוא ייפרץ, יכול להזריק קוד)
- [ ] תוכן הודעות (כולל פרטי תיק משפטי רגישים: כתובות, שמות ילדים וכו') נשלח כ-plaintext ל-`api.anthropic.com` — זה תקין מבחינת Anthropic אך תעד שאין הצפנה נוספת בצד הלקוח
- [ ] קובץ ה-Markdown export (`.txt`) — בדוק שאין הזרקת תוכן זדוני אם הטקסט המיוצא נפתח בתוכנה אחרת (Excel formula injection וכו' — פחות רלוונטי ל-.txt אך תעד אם רלוונטי)

### 14. תוכן ולוגיקה עסקית
- [ ] בדוק את ה-System Prompt המוטמע בקוד (`const SYSTEM = ...`) — האם העובדות (תאריכים, שמות) עדכניות ועקביות עם הצד השני של האפליקציה (אם יש)?
- [ ] עובדות התיק בסיידבר (Sidebar `case-fact`) — תואמות לעובדות שב-System Prompt?

---

## פורמט הדוח הסופי

הפק דוח QA מובנה בעברית, בפורמט הבא:

```markdown
# דוח QA — legal_chat.html
תאריך בדיקה: [תאריך]
כתובת שנבדקה: [URL]
דפדפן/מכשיר: [פרטים]

## תקציר מנהלים
[2-3 משפטים: מצב כללי, האם מוכן לשימוש, כמה בעיות קריטיות נמצאו]

## ממצאים לפי חומרה

### 🔴 קריטי (חוסם שימוש / סיכון אבטחה ממשי)
| # | ממצא | השפעה | המלצה |
|---|---|---|---|

### 🟠 גבוה (פוגע בחוויית משתמש משמעותית)
| # | ממצא | השפעה | המלצה |
|---|---|---|---|

### 🟡 בינוני (באג קל / שיפור UX)
| # | ממצא | השפעה | המלצה |
|---|---|---|---|

### 🟢 נמוך (קוסמטי / שיפור עתידי)
| # | ממצא | השפעה | המלצה |
|---|---|---|---|

## בדיקות שעברו בהצלחה ✅
[רשימה קצרה]

## בדיקות שלא ניתן היה לבצע ⛔
[למשל: קריאות API חיות ללא מפתח אמיתי]

## ציוני Lighthouse
- Performance: __/100
- Accessibility: __/100
- Best Practices: __/100

## המלצה סופית
[האם מוכן לשימוש על ידי אסתי? אילו תיקונים חובה לפני שימוש בתיק משפטי אמיתי?]
```

**חשוב:** תעדף תמיד את ממצאי האבטחה והנגישות במובייל — האפליקציה מיועדת למשתמשת לא-טכנית בטלפון נייד, העוסקת בתיק משפטי רגיש ביותר (אלימות במשפחה, משמורת ילדים). כל פגם שמונע ממנה לתפעל את הכלי בעצמה הוא קריטי.
